package classe;

public class ValorNulo {
    public static void main(String[] args) {
        String s1 = "";
        System.out.println(s1.concat("!!!!!"));

        /*
        Data d1 = null; //NullPointExeption
        d1.mes = 3;

        String s2 = null; //NullPointExeption
        System.out.println(s2.concat("?????"));

        // null -> não aponta para nenhum objeto na memória
        // Por isso recomend-se testar se a variável aponta para um objeto null
        // antes de tentar acessar os atributos desses objeto, para vitar o erro
        // NullPointException. Ex:
        if (d1 != null){
            d1.mes = 3;
            }
        */
    }
} 
