DELETE from `estados`
WHERE sigla = 'MN'

SELECT * from `estados`

DELETE from estados WHERE id >= 1000 -- apagando um intervalo dede linhas por IDs

SELECT * FROM estados

-- DML - data malipulation language